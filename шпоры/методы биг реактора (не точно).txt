🚀 Методы, связанные с топливом
getFuelAmountMax() — Возвращает максимальный объём топлива, который может храниться в реакторе.  
getFuelAmount() — Возвращает текущее количество топлива в реакторе.  
doEjectFuel() — Вероятно, выполняет аварийный выброс топлива или его выгрузку.  
doInjectFuel() — Возможно, метод для подачи топлива в реактор.  
getFuelReactivity() — Возможно, измеряет эффективность топлива или уровень его активности.  

🔥 Методы, связанные с горячими жидкостями и охлаждением
getHotFluidAmount() — Количество горячей жидкости (например, горячего охлаждающего агента).  
getHotFluidAmountMax() — Максимально допустимый объём горячей жидкости.  
getHotFluidType() — Тип горячей жидкости (например, пар или расплавленный металл).  
getCoolantAmount() — Количество охлаждающей жидкости в реакторе.  
getCoolantAmountMax() — Максимальное количество охлаждающей жидкости.  
getCoolantType() — Тип используемой охлаждающей жидкости (например, вода, гелий и т.п.).  
isActivelyCooled() — Проверяет, включена ли активная система охлаждения (например, через теплообменники).  

⚡ Методы, связанные с энергией
getEnergyStored() — Возвращает количество энергии в хранилище реактора.  
getEnergyProducedLastTick() — Показывает, сколько энергии было произведено за последний тик.  
getFuelConsumedLastTick() — Показывает, сколько топлива было потреблено за последний тик.  
getHotFluidProducedLastTick() — Показывает, сколько горячей жидкости было создано за последний тик.  

🔎 Методы управления управляющими стержнями
getControlRodCount() — Возвращает количество управляющих стержней в реакторе.  
getControlRodLocation() — Вероятно, возвращает координаты или номер позиции управляющего стержня.  
getControlRodName() — Возвращает название управляющего стержня.  
setControlRodName() — Похоже, устанавливает имя управляющего стержня (возможно, для организации в интерфейсе).  
getControlRodLevel() — Возвращает уровень погружения управляющего стержня (0% — полностью выдвинут, 100% — полностью погружён).  
setControlRodLevel() — Устанавливает уровень погружения стержня.  

🗺️ Методы, связанные с координатами
getMaximumCoordinate() — Вероятно, возвращает максимальную координату блока структуры реактора.  
getMinimumCoordinate() — То же самое, но для минимальной координаты.  

🔋 Методы включения/выключения реактора
getActive() — Проверяет, активен ли реактор (включён/выключен).  
setActive() — Включает или выключает реактор.  

🔄 Прочие методы
type() — Вероятно, возвращает тип устройства (например, "reactor" или "turbine").  
slot() — Возможно, возвращает данные о каком-то конкретном слоте инвентаря или контролирующей ячейке.  
getWasteAmount() — Возвращает количество отходов, скопившихся в реакторе.  
